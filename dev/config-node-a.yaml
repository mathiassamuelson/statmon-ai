server:
  host: "0.0.0.0"
  port: 8100
  node_name: "dns-node-a"

statmon:
  binary: "/opt/mock-cli/statmon"
  subsystem: ""
  timeout_seconds: 60
  rules:
    deny:
      - "querystore.reset"
    allow:
      - "querystore.*"
